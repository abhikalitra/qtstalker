# qtstalker Test Dialog


###################################################################

$|++;

$command = "PLUGIN=TEST_DIALOG";
print STDOUT $command;
#$rc = <STDIN>; chomp($rc); if ($rc eq "ERROR") {print STDERR $command; exit; }
