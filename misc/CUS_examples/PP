enable=0
plugin=CUS
tabRow=1
dateFlag=1
Name=PP
script=bars := BARS(Bar) // price bars|fr := PP(FR) // first resistance level|sr := PP(SR) // second resistance level|tr := PP(TR) // third resistance level|fs := PP(FS) // first support level|ss := PP(SS) // second suport level|ts := PP(TS) // third support level|plot (bars,#ff0000,Bars,Bar)|plot (fs,#ff0000,FS,Horizontal)|plot (ss,#ff0000,SS,Horizontal)|plot (ts,#ff0000,TS,Horizontal)|plot (fr,#ffff00,FR,Horizontal)|plot (sr,#ffff00,SR,Horizontal)|plot (tr,#ffff00,TR,Horizontal)
logScale=0
